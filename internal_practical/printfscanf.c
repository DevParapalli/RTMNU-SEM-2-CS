#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main() {
    int a, b;
    printf("Enter A and B");
    scanf("%d,%d", &a, &b);
    printf("Values Entered: %d & %d", a, b);
    return 0;
}